import React from "react";
import "../Pages.css"

const UserPanel = () => {
    return (
        <div className={'custom-background-sections'}>
            Panel de Usuario
        </div>
    );
};

export default UserPanel;
